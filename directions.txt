Install pip =>
In terminal run:
curl https://bootstrap.pypa.io/get-pip.py > get-pip.py
sudo python get-pip.py

Install Requests =>
sudo pip install requests

Make sure to import needed packages into the files being used =>
import requests
import pprint (auto installed on mac, not needed to be installed)
import json
